<p style="color: green"><%= notice %></p>

<% content_for :title, t('books.index.title') %>

<h1><%= t('books.index.title') %></h1>

<div id="books">
  <% @books.each do |book| %>
    <%= render book %>
    <p>
      <%= link_to t('books.show.detail'), book %>
    </p>
  <% end %>
</div>

<%= link_to t('books.new.title'), new_book_path %>
