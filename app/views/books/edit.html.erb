<% content_for :title, t('books.edit.title') %>

<h1><%= t('books.edit.title') %></h1>

<%= render "form", book: @book %>

<br>

<div>
  <%= link_to t('books.show.edit'), @book %> |
  <%= link_to t('books.show.back'), books_path %>
</div>
